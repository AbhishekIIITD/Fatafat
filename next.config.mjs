/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: false,
  images: {
    domains: ['images.pexels.com', 'encrypted-tbn0.gstatic.com', 'hips.hearstapps.com', 'upload.wikimedia.org','encrypted-tbn0.gstatic.com', 'example.com'],
  },
};

export default nextConfig;
